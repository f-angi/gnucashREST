swagger: "2.0"
info:
  description: This is the swagger file that goes with our server code
  version: "1.0.0"
  title: Swagger REST Article
consumes:
  - "application/json"
produces:
  - "application/json"

basePath: "/api"

# Paths supported by the server application
paths:
  /transactions:
    get:
      operationId: "models.transactions.read_all"
      tags:
        - "Transactions"
      responses:
        200:
          description: "Successful read transactions list operation"
          schema:
            type: "array"
            items:
              properties:
                guid:
                  type: "integer"
                enter_date:
                  type: "string"
                description:
                  type: "string"
